<!DOCTYPE html>
<html lang="ja">
 <head>
  <meta charset="UTF-8" />
  <title>Bookers</title>
  <link rel="stylesheet" media="all" href="application.css">
 </head>
  
  <body>
   <h1>Books</h1>
   <% @books.each do |book| %>
		<tr>
			<td><%= book.title %></td>
			<td><%= book.body %></td>
  </tr>
   
 
  <%= form_with model: @book do |f| %>
   <h2>New book</h2>
   
   
   <h4>本のタイトル</h4>
   <%= f.text_field :title %>
	
   <h4>感想</h4>
   <%= f.text_area :body %>
	
   <%= f.submit 'create book' %>
   
   <% end %>
    
  </body>
</html>